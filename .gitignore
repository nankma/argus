__pycache__/
*.pyc
notes.jsonl
subscribers.db
.claude/settings.local.json
.pytest_cache/
.coverage
*.out.log
*.err.log
*.pid.txt

# generated showcase output (build with tools/build_showcase.py)
showcase.html

# Local-only infrastructure notes (real IPs, OCIDs, SSH key paths, live
# secret values) -- must NEVER be committed or pushed. See
# local-infra/infrastructure.md's own warning banner for why this exists
# and the rules around it.
local-infra/

# docs/analysis/ build artifacts -- regenerate with docs/analysis/tools/
# (see docs/analysis/README.md).
# The report is a ~700KB generated file; the snapshot is a point-in-time pull
# from the production VM. Findings that outlive both live in
# docs/analysis/cluster-measurements.md.
docs/analysis/cluster-report.html
docs/analysis/data/
